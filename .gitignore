# ── Build artifacts ──────────────────────────────────────────────────────────
/target
/classes
*.jar
*.class

# ── Leiningen (legacy) ───────────────────────────────────────────────────────
/lib
/checkouts
pom.xml
.lein-deps-sum
.lein-failures
.lein-plugins
.lein*

# ── Clojure deps.edn tooling ─────────────────────────────────────────────────
/.cpcache

# ── ClojureScript / Shadow-cljs build output ─────────────────────────────────
/.shadow-cljs
/node_modules
/resources/public/js

# ── npm ──────────────────────────────────────────────────────────────────────
package-lock.json

# ── Runtime data (codescene-lite EDN store) ──────────────────────────────────
/data

# ── Editor / IDE ─────────────────────────────────────────────────────────────
.idea/
*.iml
.DS_Store
*.txt#
.#*
*.clj#

# ── REPL / tooling ───────────────────────────────────────────────────────────
.nrepl-port
.portal/
.claude/

# ── Temp / scratch ───────────────────────────────────────────────────────────
*~
mytodo.txt
test_data.txt
# Note: *.csv intentionally NOT ignored — test fixture files are tracked
